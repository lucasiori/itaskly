export type SidebarMenuProps = {
  isOpened: boolean;
  onClose: () => void;
};

export type StyledSidebarMenuOverlayProps = {
  $isMenuOpened: boolean;
};

export type StyledSidebarMenuContainerProps = {
  $isMenuOpened: boolean;
};
